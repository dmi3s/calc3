# Calc3 — Plan

> Рабочий план развития проекта. Пункты будут наполняться по мере проработки.

## Язык / парсер

- [x] Операторы присваивания / связывания (`let`)
  - связывание во время вычисления (в текущий scope); перепривязка разрешена
- [x] Таблица символов (`src/symbol_table.c3`, модуль `calc3::symbol_table`)
  - единственное глобальное пространство имён (`global`) + эфемерные call-фреймы для параметров/рекурсии
  - хранение: `HashMap { String, VarSymbol }` и `HashMap { String, FunctionDef }`
- [x] Пространства имён (namespaces)
  - пока только `global`; интерфейс под несколько — позже
  - [x] Функции
   - [x] Определение функций — заголовки в формате C3 (prefix):
         `fn <RetType> <name>(<ParamType> <param>, ...) { ... }`
     - параметры — явно перечисленные, всегда по копии (по значению)
     - типизация: пока только `int`; неподдерживаемый тип → `ParseError("Unsupported type '...'")`
     - один вывод (один возвращаемый тип); проверка типов аргументов/возврата пока по соглашению (все `int`)
     - рекурсия разрешена (call-фреймы)
     - без модулей (только локальные имена) на текущем этапе
   - [x] Вызовы функций (использование)
     - явный список аргументов `(a, b, ...)`; несовпадение количества аргументов → `EvalError("Argument count mismatch")`
   - [x] Встроенная функция `ask()` — чтение `int` из stdin с приглашением `? `
   - [ ] (добавить по ходу)

## Прочее

- [ ] (добавить)
